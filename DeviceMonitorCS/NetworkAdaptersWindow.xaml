<Window x:Class="DeviceMonitorCS.NetworkAdaptersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Network Adapters Manager" Height="550" Width="1000" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <!-- Toolbar -->
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button x:Name="UninstallWifiDirectBtn" Content="Uninstall Microsoft WiFi Direct Virtual Adapter" Margin="5"/>
                <Separator/>
                <Button x:Name="UninstallWanMiniportBtn" Content="Uninstall All WAN Miniport" Margin="5"/>
                <Separator/>
                <Button x:Name="UninstallBluetoothPanBtn" Content="Uninstall Bluetooth Device (Personal Area Network)" Margin="5"/>
                <Separator/>
                <Button x:Name="UninstallIntelWifiBtn" Content="Uninstall Intel WiFi 6E AX210" Margin="5"/>
                <Separator/>
                <Button x:Name="RefreshAdaptersBtn" Content="Refresh" Margin="5" Width="70"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Network Adapters Grid -->
        <GroupBox DockPanel.Dock="Top" Header="Network Adapters" Padding="5" Margin="5">
            <DataGrid x:Name="NetworkAdaptersGrid" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="250"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                    <DataGridTextColumn Header="MAC Address" Binding="{Binding MacAddress}" Width="150"/>
                    <DataGridTextColumn Header="Interface Type" Binding="{Binding InterfaceType}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </DockPanel>
</Window>
