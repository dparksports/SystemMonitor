<UserControl x:Class="DeviceMonitorCS.Views.DeviceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="RowBackground" Value="#252526"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AlternatingRowBackground" Value="#2D2D30"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="25,20,25,10">
            <StackPanel>
                <TextBlock Text="Device Management" FontSize="26" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                <TextBlock Text="Inventory of Keyboards, Mice, and Monitors via Get-PnpDevice logic." FontSize="14" Foreground="#888888" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Stats and Controls -->
        <Border Grid.Row="1" Margin="25,10,25,15" Padding="15" Background="#1E1E1E" CornerRadius="8">
             <Grid>
                 <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                     <StackPanel Margin="0,0,30,0">
                         <TextBlock Text="Total Tracked" Foreground="#888888" FontSize="12"/>
                         <TextBlock x:Name="TotalDeviceCount" Text="0" Foreground="Cyan" FontSize="24" FontWeight="Bold">
                             <TextBlock.Effect>
                                 <DropShadowEffect Color="Cyan" BlurRadius="5" ShadowDepth="0" Opacity="0.5"/>
                             </TextBlock.Effect>
                         </TextBlock>
                     </StackPanel>
                     <StackPanel Margin="0,0,30,0">
                         <TextBlock Text="OK Status" Foreground="#888888" FontSize="12"/>
                         <TextBlock x:Name="ActiveDeviceCount" Text="0" Foreground="LimeGreen" FontSize="24" FontWeight="Bold"/>
                     </StackPanel>
                 </StackPanel>
                 
                 <Button x:Name="RefreshBtn" Content="Refresh Devices" HorizontalAlignment="Right" Padding="20,8" 
                         Background="#333333" Foreground="White" BorderThickness="1" BorderBrush="#555" 
                         Cursor="Hand" Click="RefreshBtn_Click">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                 </Button>
             </Grid>
        </Border>

        <!-- Inventory List -->
        <Border Grid.Row="2" Margin="25,0,25,25" Background="#1E1E1E" CornerRadius="8" Padding="2" BorderThickness="1" BorderBrush="#333">
            <DataGrid x:Name="DeviceGrid" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                         <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="OK">
                                        <Setter Property="Foreground" Value="LimeGreen"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Unknown">
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Friendly Name" Binding="{Binding FriendlyName}" Width="300"/>
                    <DataGridTextColumn Header="Instance ID" Binding="{Binding InstanceId}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
