<UserControl x:Class="DeviceMonitorCS.Views.DeviceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="RowBackground" Value="#252526"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AlternatingRowBackground" Value="#2D2D30"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="25,20,25,0">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Device Management" FontSize="26" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                    <TextBlock Text="Manage connected and disconnected Keyboards, Mice, and Monitors." FontSize="14" Foreground="#888888" Margin="0,5,0,0"/>
                </StackPanel>
                 <Button x:Name="RefreshBtn" Content="Refresh Lists" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="15,6" 
                         Background="#333333" Foreground="White" BorderThickness="1" BorderBrush="#555" 
                         Cursor="Hand" Click="RefreshBtn_Click">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                 </Button>
            </Grid>
        </Border>

        <!-- Stats (Optional, kept minimal) -->
        <Border Grid.Row="1" Margin="25,10,25,10" Padding="0">
             <!-- Could add aggregates here later if needed -->
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="25,0,25,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="15"/> 
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top: Connected Devices -->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Connected Devices" FontSize="16" FontWeight="Bold" Foreground="#4CAF50"/>
                    <TextBlock x:Name="ConnectedCountText" Text="(0)" FontSize="16" Foreground="#888" Margin="5,0,0,0"/>
                </StackPanel>
                
                <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="6" BorderThickness="1" BorderBrush="#333">
                    <DataGrid x:Name="ConnectedGrid" IsReadOnly="True" ItemsSource="{Binding ConnectedList}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="LimeGreen"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Friendly Name" Binding="{Binding FriendlyName}" Width="300"/>
                            <DataGridTextColumn Header="Instance ID" Binding="{Binding InstanceId}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <!-- Splitter/Spacer -->

            <!-- Bottom: Disconnected Devices -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Disconnected Devices" FontSize="16" FontWeight="Bold" Foreground="#FF5252"/>
                    <TextBlock x:Name="DisconnectedCountText" Text="(0)" FontSize="16" Foreground="#888" Margin="5,0,0,0"/>
                    <TextBlock Text="(Includes all non-OK statuses)" FontSize="12" Foreground="#666" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="6" BorderThickness="1" BorderBrush="#333">
                    <DataGrid x:Name="DisconnectedGrid" IsReadOnly="True" ItemsSource="{Binding DisconnectedList}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                                 <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Friendly Name" Binding="{Binding FriendlyName}" Width="300"/>
                            <DataGridTextColumn Header="Instance ID" Binding="{Binding InstanceId}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
