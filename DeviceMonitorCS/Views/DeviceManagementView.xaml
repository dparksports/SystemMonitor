<UserControl x:Class="DeviceMonitorCS.Views.DeviceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
         <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="RowBackground" Value="#1E1E1E"/>
            <Setter Property="AlternatingRowBackground" Value="#252525"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#A0A0A0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#404040"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#303050"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Padding="15,10" Background="Transparent" BorderThickness="0,0,0,2" BorderBrush="Transparent">
                            <ContentPresenter ContentSource="Header" TextElement.Foreground="#888888" TextElement.FontSize="14" TextElement.FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/> 
                                <!-- Note: Foreground on TabItem doesn't automatically propagate to ContentPresenter in this template effectively without explicit binding, 
                                     but triggers usually handle TextElement attached props. Simplified for now. -->
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#252525"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="25,20,25,10">
            <StackPanel>
                <TextBlock Text="Device Management" FontSize="26" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                <TextBlock Text="Inventory and history for Keyboards, Mice, and Monitors." FontSize="14" Foreground="#888888" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Stats -->
        <Border Grid.Row="1" Margin="25,0,25,15" Padding="15" Background="#1E1E1E" CornerRadius="8">
             <StackPanel Orientation="Horizontal">
                 <StackPanel Margin="0,0,30,0">
                     <TextBlock Text="Keyboards" Foreground="#888888" FontSize="12"/>
                     <TextBlock x:Name="KeyboardCountFormatted" Text="0" Foreground="White" FontSize="24" FontWeight="Bold"/>
                 </StackPanel>
                 <StackPanel Margin="0,0,30,0">
                     <TextBlock Text="Mice" Foreground="#888888" FontSize="12"/>
                     <TextBlock x:Name="MouseCountFormatted" Text="0" Foreground="White" FontSize="24" FontWeight="Bold"/>
                 </StackPanel>
                 <StackPanel>
                     <TextBlock Text="Monitors" Foreground="#888888" FontSize="12"/>
                     <TextBlock x:Name="MonitorCountFormatted" Text="0" Foreground="White" FontSize="24" FontWeight="Bold"/>
                 </StackPanel>
                 
                 <Button x:Name="RefreshBtn" Content="Refresh" Margin="50,0,0,0" VerticalAlignment="Center" Padding="15,5" Background="#333333" Foreground="White" BorderThickness="0" Click="RefreshBtn_Click"/>
             </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="25,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/> <!-- Inventory -->
                <RowDefinition Height="5"/>  <!-- Splitter -->
                <RowDefinition Height="2*"/> <!-- History -->
            </Grid.RowDefinitions>

            <!-- Master: Inventory -->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="All Devices" FontSize="16" Foreground="#CCCCCC"/>
                    <TextBlock Text="(Select a device to view history)" FontSize="12" Foreground="#888888" VerticalAlignment="Bottom" Margin="10,0,0,2"/>
                </StackPanel>
                
                <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="8" Padding="2">
                    <DataGrid x:Name="InventoryGrid" ItemsSource="{Binding InventoryList}" SelectionChanged="InventoryGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                            <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="150"/>
                             <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Status}" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="LimeGreen"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                            <Setter Property="Foreground" Value="Gray"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Disconnected">
                                                            <Setter Property="Foreground" Value="#AA4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Device ID" Binding="{Binding DeviceId}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <!-- Splitter Visual -->
            <Border Grid.Row="1" Background="Transparent"/>

            <!-- Detail: History -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="0,15,0,10">
                     <TextBlock Text="Device History" FontSize="16" Foreground="#CCCCCC"/>
                     <TextBlock x:Name="SelectedDeviceText" Text="(All Events)" FontSize="14" Foreground="#888888" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                
                <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="8" Padding="2">
                    <DataGrid x:Name="HistoryGrid" ItemsSource="{Binding HistoryList}">
                         <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="140"/>
                            <DataGridTextColumn Header="Event" Binding="{Binding EventName}" Width="120"/>
                            <DataGridTextColumn Header="Device Name" Binding="{Binding DeviceName}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
