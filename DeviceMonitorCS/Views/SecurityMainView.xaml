<UserControl x:Class="DeviceMonitorCS.Views.SecurityMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sub-Navigation -->
        <Border Grid.Column="0" Background="#11FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="0,0,1,0" Margin="0,0,0,0">
            <StackPanel Margin="10,20,10,10">
                <TextBlock Text="SECURITY MODULES" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="10,0,0,15"/>
                
                <Button x:Name="OverviewBtn" Content="System Overview" Click="OverviewBtn_Click" Style="{StaticResource NavButton}"/>
                <Button x:Name="TasksBtn" Content="↳ Scheduled Tasks" Click="TasksBtn_Click" Style="{StaticResource NavButton}" Margin="25,4,10,2" Height="35" FontSize="12" Opacity="0.8"/>
                <Button x:Name="ConnBtn" Content="↳ Connections" Click="ConnBtn_Click" Style="{StaticResource NavButton}" Margin="25,2,10,2" Height="35" FontSize="12" Opacity="0.8"/>
                <Button x:Name="FirewallBtn" Content="↳ Firewall Settings" Click="FirewallBtn_Click" Style="{StaticResource NavButton}" Margin="25,2,10,2" Height="35" FontSize="12" Opacity="0.8"/>
                <Button x:Name="PrivacyBtn" Content="↳ Privacy Settings" Click="PrivacyBtn_Click" Style="{StaticResource NavButton}" Margin="25,2,10,2" Height="35" FontSize="12" Opacity="0.8"/>
                <Button x:Name="EventsBtn" Content="↳ Event Management" Click="EventsBtn_Click" Style="{StaticResource NavButton}" Margin="25,2,10,2" Height="35" FontSize="12" Opacity="0.8"/>
                <Button x:Name="ColdBootsBtn" Content="↳ Cold Boots" Click="ColdBootsBtn_Click" Style="{StaticResource NavButton}" Margin="25,2,10,8" Height="35" FontSize="12" Opacity="0.8"/>

                <Separator Background="#22FFFFFF" Margin="10,10,10,10"/>

                <Button x:Name="DefenderBtn" Content="Windows Defender" Click="DefenderBtn_Click" Style="{StaticResource NavButton}"/>
                <Button x:Name="PanelBtn" Content="Command Panel" Click="PanelBtn_Click" Style="{StaticResource NavButton}"/>
            </StackPanel>
        </Border>

        <!-- Sub-Content -->
        <Border Grid.Column="1" Margin="20">
            <ContentControl x:Name="SubContentArea"/>
        </Border>
    </Grid>
</UserControl>
