<UserControl x:Class="DeviceMonitorCS.Views.ColdBootsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceMonitorCS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <TextBlock Text="Cold Boots" FontSize="24" FontWeight="Bold" Foreground="Cyan">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Cyan" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <Button x:Name="RefreshBtn" Content="Refresh" Margin="20,0,0,0" Padding="10,5" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" Click="RefreshBtn_Click"/>
            </StackPanel>
        </Grid>

        <!-- Tools Section -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Hibernation Settings -->
            <GroupBox Header="Hibernation Settings" Grid.Column="0" Margin="0,0,10,0" Padding="15" BorderBrush="#3E3E42" Foreground="White">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Status: " Foreground="#CCCCCC" VerticalAlignment="Center"/>
                        <TextBlock x:Name="HibernateStatusText" Text="Checking..." FontWeight="SemiBold" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                    <Button x:Name="ToggleHibernateBtn" Content="Toggle Hibernation" HorizontalAlignment="Left" Padding="10,5" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" Click="ToggleHibernateBtn_Click"/>
                </StackPanel>
            </GroupBox>

            <!-- True Shutdown -->
            <GroupBox Header="True Shutdown" Grid.Column="1" Margin="10,0,0,0" Padding="15" BorderBrush="#3E3E42" Foreground="White">
                <StackPanel>
                    <TextBlock Text="Forces full shutdown (bypasses Fast Startup)" Foreground="#CCCCCC" Margin="0,0,0,10" FontSize="11" TextWrapping="Wrap"/>
                    <Button x:Name="TrueShutdownBtn" Content="Perform True Shutdown" HorizontalAlignment="Left" Padding="10,5" Background="#FF6B6B" Foreground="White" BorderBrush="Red" Click="TrueShutdownBtn_Click" FontWeight="Bold"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <!-- Boot List -->
        <DataGrid x:Name="BootsGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                  EnableRowVirtualization="True"
                  Background="#252526" Foreground="White" RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Boot Time" Binding="{Binding BootTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                <DataGridTextColumn Header="Shutdown Time" Binding="{Binding ShutdownTimeStr}" Width="160"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Uptime" Binding="{Binding UptimeStr}" Width="140"/>
                <DataGridTextColumn Header="KG12" Binding="{Binding Kg12Time}" Width="100"/>
                <DataGridTextColumn Header="Sec4608" Binding="{Binding Sec4608Str}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
