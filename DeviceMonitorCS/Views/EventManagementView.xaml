<UserControl x:Class="DeviceMonitorCS.Views.EventManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Base DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="RowBackground" Value="#2D2D30"/>
            <Setter Property="AlternatingRowBackground" Value="#252526"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#333337"/>
                        <Setter Property="Foreground" Value="#E0E0E0"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#3F3F46"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>   <!-- Security Events -->
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>   <!-- Device Events -->
        </Grid.RowDefinitions>

        <!-- Security Events -->
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Security Events" Style="{StaticResource HeaderLabel}"/>
            
            <DataGrid x:Name="SecurityGrid" Grid.Row="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="70"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding Account}" Width="100"/>
                    <DataGridTextColumn Header="Event" Binding="{Binding Type}" Width="80"/>
                    
                    <DataGridTextColumn Header="Account Name" Binding="{Binding AccountName}" Width="100"/>
                    <DataGridTextColumn Header="Account Domain" Binding="{Binding AccountDomain}" Width="100"/>
                    <DataGridTextColumn Header="Logon ID" Binding="{Binding LogonId}" Width="70"/>
                    <DataGridTextColumn Header="Security ID" Binding="{Binding SecurityId}" Width="120"/>

                    <!-- Privilege Flags -->
                    <DataGridCheckBoxColumn Header="AssignTok" Binding="{Binding SeAssignPrimaryTokenPrivilege}" Width="65"/>
                    <DataGridCheckBoxColumn Header="TCB" Binding="{Binding SeTcbPrivilege}" Width="40"/>
                    <DataGridCheckBoxColumn Header="Securt" Binding="{Binding SeSecurityPrivilege}" Width="50"/>
                    <DataGridCheckBoxColumn Header="TakeOw" Binding="{Binding SeTakeOwnershipPrivilege}" Width="55"/>
                    <DataGridCheckBoxColumn Header="LoadDr" Binding="{Binding SeLoadDriverPrivilege}" Width="50"/>
                    <DataGridCheckBoxColumn Header="Backup" Binding="{Binding SeBackupPrivilege}" Width="50"/>
                    <DataGridCheckBoxColumn Header="Restor" Binding="{Binding SeRestorePrivilege}" Width="50"/>
                    <DataGridCheckBoxColumn Header="Deb" Binding="{Binding SeDebugPrivilege}" Width="40"/>
                    <DataGridCheckBoxColumn Header="Audi" Binding="{Binding SeAuditPrivilege}" Width="40"/>
                    <DataGridCheckBoxColumn Header="SysEnv" Binding="{Binding SeSystemEnvironmentPrivilege}" Width="50"/>
                    <DataGridCheckBoxColumn Header="Impers" Binding="{Binding SeImpersonatePrivilege}" Width="50"/>

                    <DataGridTextColumn Header="Logon Typ" Binding="{Binding LogonType}" Width="70"/>
                    <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="150"/>
                    <DataGridTextColumn Header="Workstation" Binding="{Binding WorkstationName}" Width="100"/>
                    <DataGridTextColumn Header="Source IP" Binding="{Binding IpAddress}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Device Events -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Device Events" Style="{StaticResource HeaderLabel}"/>

            <DataGrid x:Name="DeviceGrid" Grid.Row="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="80"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding Name}" Width="250"/>
                    <DataGridTextColumn Header="Event" Binding="{Binding Type}" Width="150"/>
                    <DataGridTextColumn Header="Details" Binding="{Binding EventType}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

    </Grid>
</UserControl>
