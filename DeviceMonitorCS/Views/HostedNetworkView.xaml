<UserControl x:Class="DeviceMonitorCS.Views.HostedNetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Hosted Network Manager" FontSize="24" Foreground="White" Margin="0,0,0,20"/>

        <!-- Toolbar area -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="StopHostedNetworkBtn" Content="Stop HostedNetwork"/>
            <Button x:Name="DisableHostedNetworkBtn" Content="Disable HostedNetwork"/>
            <Button x:Name="DisableWifiDirectBtn" Content="Disable WiFi Direct Virtual Adapter"/>
            <Button x:Name="DisableAx210Btn" Content="Disable Intel WiFi 6E AX210"/>
            <Button x:Name="RefreshBtn" Content="Refresh" Background="#007ACC"/>
        </WrapPanel>

        <!-- Grid -->
        <Border Grid.Row="2" Background="#252526" CornerRadius="5" Padding="5">
            <DataGrid x:Name="AdaptersGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      Background="Transparent" BorderThickness="0" Foreground="White" RowBackground="#252526" AlternatingRowBackground="#2D2D30">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="250"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding InterfaceType}" Width="150"/>
                    <DataGridTextColumn Header="DeviceID" Binding="{Binding DeviceID}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch" Background="#3E3E42" Margin="0,5"/>

        <!-- Console -->
        <Border Grid.Row="4" Background="Black" CornerRadius="5" Padding="5">
            <TextBox x:Name="ConsoleOutput" IsReadOnly="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Background="Transparent" Foreground="#00FF00" BorderThickness="0"/>
        </Border>
    </Grid>
</UserControl>
