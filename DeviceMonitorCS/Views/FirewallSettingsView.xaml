<UserControl x:Class="DeviceMonitorCS.Views.FirewallSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceMonitorCS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="#1E1E1E">
    <UserControl.Resources>
        <local:GroupToEnabledStateConverter x:Key="GroupToEnabledStateConverter"/>
        
        <CollectionViewSource x:Key="InboundRulesSource" Source="{Binding InboundRules, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="DisplayGroup"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
        
        <CollectionViewSource x:Key="OutboundRulesSource" Source="{Binding OutboundRules, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="DisplayGroup"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <TextBlock Text="Firewall Settings" FontSize="24" FontWeight="Bold" Foreground="Cyan">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Cyan" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <Button x:Name="RefreshBtn" Content="Refresh" Margin="20,0,0,0" Padding="10,5" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" Click="RefreshBtn_Click"/>
            </StackPanel>

            <Button x:Name="ResetBtn" Grid.Column="1" Content="Reset to Default" Padding="15,5" Background="#FF4444" Foreground="White" BorderBrush="Red" Click="ResetBtn_Click" FontWeight="Bold"/>
        </Grid>

        <!-- Rules Tabs -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#252526"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="15,10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                                        <Setter Property="Foreground" Value="Cyan"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Inbound Rules">
                <DataGrid x:Name="InboundGrid" ItemsSource="{Binding Source={StaticResource InboundRulesSource}}" 
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                          EnableRowVirtualization="False"
                          Background="#252526" Foreground="White" RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" Margin="0,10,0,0">
                    <DataGrid.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.ContainerStyle>
                                <Style TargetType="{x:Type GroupItem}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type GroupItem}">
                                                <Expander IsExpanded="False" Background="#333333" Foreground="White" BorderBrush="Gray" BorderThickness="0,0,0,1">
                                                    <Expander.Header>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <CheckBox VerticalAlignment="Center" Margin="0,0,10,0"
                                                                      IsChecked="{Binding Items, Converter={StaticResource GroupToEnabledStateConverter}, Mode=OneWay}"
                                                                      Click="GroupToggle_Click" Tag="{Binding Name}">
                                                                 <CheckBox.LayoutTransform>
                                                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                                                </CheckBox.LayoutTransform>
                                                            </CheckBox>
                                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="Cyan" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding ItemCount, StringFormat=' ({0} rules)'}" FontStyle="Italic" Foreground="LightGray" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                        </StackPanel>
                                                    </Expander.Header>
                                                    <ItemsPresenter />
                                                </Expander>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </GroupStyle.ContainerStyle>
                        </GroupStyle>
                    </DataGrid.GroupStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="Group" Binding="{Binding DisplayGroup}" Width="1.5*"/>
                        <DataGridTextColumn Header="Profile" Binding="{Binding Profile}" Width="1*"/>
                        <DataGridTextColumn Header="Program" Binding="{Binding Program}" Width="2*"/>
                        <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="0.8*"/>
                        <DataGridTextColumn Header="Local Port" Binding="{Binding LocalPort}" Width="0.8*"/>
                        <DataGridTextColumn Header="Remote Port" Binding="{Binding RemotePort}" Width="0.8*"/>
                        <DataGridTextColumn Header="Remote Address" Binding="{Binding RemoteAddress}" Width="1.5*"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="0.8*"/>
                        <DataGridTextColumn Header="Enabled" Binding="{Binding Enabled}" Width="0.8*"/>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Toggle" Click="ToggleRule_Click" CommandParameter="{Binding}" 
                                            Background="#007ACC" Foreground="White" BorderThickness="0" Padding="5,2" Margin="2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Toggle Rule" Click="ToggleRuleCtx_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>
            </TabItem>

            <TabItem Header="Outbound Rules">
                <DataGrid x:Name="OutboundGrid" ItemsSource="{Binding Source={StaticResource OutboundRulesSource}}" 
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                          EnableRowVirtualization="False"
                          Background="#252526" Foreground="White" RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderThickness="0" Margin="0,10,0,0">
                    <DataGrid.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.ContainerStyle>
                                <Style TargetType="{x:Type GroupItem}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type GroupItem}">
                                                <Expander IsExpanded="False" Background="#333333" Foreground="White" BorderBrush="Gray" BorderThickness="0,0,0,1">
                                                    <Expander.Header>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                             <CheckBox VerticalAlignment="Center" Margin="0,0,10,0"
                                                                      IsChecked="{Binding Items, Converter={StaticResource GroupToEnabledStateConverter}, Mode=OneWay}"
                                                                      Click="GroupToggle_Click" Tag="{Binding Name}">
                                                                <CheckBox.LayoutTransform>
                                                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                                                </CheckBox.LayoutTransform>
                                                            </CheckBox>
                                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="Cyan" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding ItemCount, StringFormat=' ({0} rules)'}" FontStyle="Italic" Foreground="LightGray" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                        </StackPanel>
                                                    </Expander.Header>
                                                    <ItemsPresenter />
                                                </Expander>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </GroupStyle.ContainerStyle>
                        </GroupStyle>
                    </DataGrid.GroupStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="Group" Binding="{Binding DisplayGroup}" Width="1.5*"/>
                        <DataGridTextColumn Header="Profile" Binding="{Binding Profile}" Width="1*"/>
                        <DataGridTextColumn Header="Program" Binding="{Binding Program}" Width="2*"/>
                        <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="0.8*"/>
                        <DataGridTextColumn Header="Local Port" Binding="{Binding LocalPort}" Width="0.8*"/>
                        <DataGridTextColumn Header="Remote Port" Binding="{Binding RemotePort}" Width="0.8*"/>
                        <DataGridTextColumn Header="Remote Address" Binding="{Binding RemoteAddress}" Width="1.5*"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="0.8*"/>
                        <DataGridTextColumn Header="Enabled" Binding="{Binding Enabled}" Width="0.8*"/>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Toggle" Click="ToggleRule_Click" CommandParameter="{Binding}" 
                                            Background="#007ACC" Foreground="White" BorderThickness="0" Padding="5,2" Margin="2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Toggle Rule" Click="ToggleRuleCtx_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
