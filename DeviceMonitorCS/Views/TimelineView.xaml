<UserControl x:Class="DeviceMonitorCS.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctrl="clr-namespace:DeviceMonitorCS.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366"
             Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header Dashboard (120px) -->
            <RowDefinition Height="*"/>    <!-- Main Content -->
            <RowDefinition Height="Auto"/> <!-- Footer (40px) -->
        </Grid.RowDefinitions>

        <!-- A. HEADER & DASHBOARD -->
        <Border Grid.Row="0" Background="#252526" BorderBrush="#3E3E42" BorderThickness="0,0,0,1" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Title & Date -->
                <StackPanel Orientation="Horizontal" Visibility="Visible">
                     <TextBlock Text="SYSTEM TIMELINE" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                     <DatePicker x:Name="EventDatePicker" Width="150" VerticalAlignment="Center"/>
                     <Button x:Name="RefreshBtn" Content="↻ Refresh" Margin="10,0,0,0" Padding="10,5" Click="RefreshBtn_Click"/>
                </StackPanel>
                
                <!-- Metrics (Placeholders for now) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Metric Card 1 -->
                    <Border Background="#333" CornerRadius="5" Padding="15,5" Margin="5">
                        <StackPanel>
                            <TextBlock Text="TOTAL EVENTS" FontSize="10" Foreground="Gray"/>
                            <TextBlock x:Name="MetricTotal" Text="0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>
                    </Border>
                    <!-- Metric Card 2 -->
                    <Border Background="#333" CornerRadius="5" Padding="15,5" Margin="5">
                        <StackPanel>
                            <TextBlock Text="ERRORS" FontSize="10" Foreground="Gray"/>
                            <TextBlock x:Name="MetricErrors" Text="0" FontSize="20" FontWeight="Bold" Foreground="#FF5555"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- B. C. D. MAIN CONTENT (Holy Grail) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/> <!-- Sidebar -->
                <ColumnDefinition Width="*"/>   <!-- Canvas -->
                <ColumnDefinition Width="350"/> <!-- Detail -->
            </Grid.ColumnDefinitions>

            <!-- B. LEFT SIDEBAR (Filters) -->
            <ctrl:TimelineFilter x:Name="TimelineFilterControl" Grid.Column="0"/>

            <!-- C. PRIMARY TIMELINE CANVAS -->
            <Border Grid.Column="1" Background="#111" ClipToBounds="True">
                <ctrl:TimelineCanvas x:Name="MainTimelineCanvas"/>
            </Border>

            <!-- D. RIGHT DETAIL PANEL -->
            <Border Grid.Column="2" Background="#252526" BorderBrush="#3E3E42" BorderThickness="1,0,0,0">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="EVENT DETAILS" FontWeight="Bold" Foreground="Gray" Margin="0,0,0,10"/>
                    
                    <!-- Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="DetailPanelContent">
                            <TextBlock Text="Select an event to inspect." Foreground="#666" FontStyle="Italic"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- E. FOOTER -->
        <Border Grid.Row="2" Background="#007ACC" Padding="10,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Text="Ready" Foreground="White" FontWeight="SemiBold"/>
                <ProgressBar x:Name="LoadingBar" Width="100" Height="10" Margin="10,0,0,0" IsIndeterminate="True" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
