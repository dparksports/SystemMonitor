<UserControl x:Class="DeviceMonitorCS.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="System Timeline" FontSize="28" FontWeight="Bold" Foreground="Cyan" Grid.Row="0" Margin="0,0,0,20">
            <TextBlock.Effect>
                <DropShadowEffect Color="Cyan" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Filter Date:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="EventDatePicker" Width="150" VerticalAlignment="Center"/>
            
            <Button x:Name="RefreshBtn" Content="Refresh Events" Width="120" Height="30" Margin="20,0,0,0" ToolTip="Reload events" Click="RefreshBtn_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#007ACC"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#009DFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <CheckBox x:Name="ShowAllDates" Content="Show All Time" Foreground="White" VerticalAlignment="Center" Margin="20,0,0,0" Click="ShowAllDates_Click"/>
        </StackPanel>

        <!-- Data Results -->
        <DataGrid x:Name="EventsGrid" Grid.Row="2" AutoGenerateColumns="False" HeadersVisibility="Column" 
                  Background="#252526" RowBackground="#252526" AlternatingRowBackground="#2D2D30" 
                  Foreground="White" BorderBrush="#3E3E42" GridLinesVisibility="Horizontal" MinRowHeight="25" IsReadOnly="True"
                  ScrollViewer.CanContentScroll="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,5"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="160"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="180"/>
                <DataGridTextColumn Header="Event ID" Binding="{Binding EventId}" Width="80"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock x:Name="StatusText" Text="Ready" Foreground="Gray"/>
            <ProgressBar x:Name="LoadingBar" Width="100" Height="10" Margin="10,0,0,0" IsIndeterminate="True" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</UserControl>
