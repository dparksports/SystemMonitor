<UserControl x:Class="DeviceMonitorCS.Views.SubscriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceMonitorCS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6366F1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#6366F1"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF2FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F9FAFB">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" Padding="30" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="Upgrade to Pro" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                <TextBlock Text="Unlock the full potential of your device monitoring." FontSize="16" Foreground="#6B7280" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                
                <!-- Status Card -->
                <Border Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Current Plan" FontSize="14" Foreground="#6B7280" FontWeight="Medium"/>
                            <TextBlock x:Name="CurrentPlanText" Text="Free Tier" FontSize="20" FontWeight="Bold" Foreground="#1F2937" Margin="0,5,0,0"/>
                            <TextBlock x:Name="ExpiryText" Text="No expiration" FontSize="12" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="#ECFDF5" CornerRadius="20" Padding="12,6" VerticalAlignment="Top">
                            <TextBlock x:Name="StatusBadge" Text="Active" Foreground="#059669" FontSize="12" FontWeight="Bold"/>
                        </Border>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Features Column -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Why Go Pro?" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>
                        
                        <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
                            <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="Unlimited Device History" Foreground="#374151"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
                            <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="Advanced Network Tools" Foreground="#374151"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
                            <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="Priority Email Support" Foreground="#374151"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
                            <TextBlock Text="✓" Foreground="#10B981" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="Cloud Sync (Coming Soon)" Foreground="#374151"/>
                        </StackPanel>

                        <TextBlock Text="$9.99 / month" FontSize="24" FontWeight="Bold" Foreground="#1F2937" Margin="0,20,0,5"/>
                        <TextBlock Text="Cancel anytime." FontSize="14" Foreground="#6B7280" Margin="0,0,0,20"/>

                        <Button Content="Subscribe via Stripe" Click="SubscribeButton_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Width="200"/>
                        <Button Content="Manage Subscription" Click="ManageSubscription_Click" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left" Width="200" Margin="0,10,0,0"/>
                        <TextBlock Text="* Requires existing subscription" FontSize="11" Foreground="#9CA3AF" Margin="5,5,0,0"/>
                    </StackPanel>

                    <!-- Activation Column -->
                    <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" Padding="20" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="Activate License" FontSize="18" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>
                            <TextBlock Text="Enter your license key from the Stripe Customer Portal." TextWrapping="Wrap" Foreground="#6B7280" Margin="0,0,0,15"/>
                            
                            <TextBlock Text="License Key" FontSize="12" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                            <TextBox x:Name="LicenseKeyInput" Padding="10" BorderBrush="#D1D5DB" BorderThickness="1" Height="40" VerticalContentAlignment="Center" FontSize="14"/>
                            
                            <Button Content="Activate" Click="ActivateButton_Click" Background="#111827" Foreground="White" Padding="0,10" Margin="0,15,0,0" FontWeight="SemiBold" BorderThickness="0">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="6"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                            
                            <TextBlock x:Name="ActivationMessage" Text="" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="13"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
