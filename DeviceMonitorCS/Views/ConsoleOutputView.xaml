<UserControl x:Class="DeviceMonitorCS.Views.ConsoleOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceMonitorCS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800"
             Height="200" BorderBrush="#3E3E42" BorderThickness="0,1,0,0">
    <UserControl.Background>
        <SolidColorBrush Color="Black" Opacity="0.95"/>
    </UserControl.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#333333" Padding="10,5">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" Foreground="#00FF00" Margin="0,0,10,0"/>
                    <TextBlock Text="AGENT EXECUTION LOG" Foreground="#00FF00" FontWeight="Bold" FontFamily="Consolas"/>
                </StackPanel>
                <Button Content="Clear" HorizontalAlignment="Right" Padding="10,2" Background="Transparent" Foreground="#888888" BorderThickness="0" Cursor="Hand" Click="ClearBtn_Click"/>
            </Grid>
        </Border>

        <!-- Console Text -->
        <TextBox x:Name="ConsoleTextBox" Grid.Row="1" 
                 Background="Transparent" Foreground="#00FF00" 
                 FontFamily="Consolas" FontSize="12"
                 IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 BorderThickness="0" Padding="10"
                 TextChanged="ConsoleTextBox_TextChanged"/>
    </Grid>
</UserControl>
