<UserControl x:Class="DeviceMonitorCS.Views.PrivacyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E">
    <UserControl.Resources>
        <!-- Modern Toggle Switch Style for CheckBox -->
        <Style x:Key="ToggleSwitchStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#333"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="SwitchBorder" Width="40" Height="20" CornerRadius="10" Background="#333" BorderBrush="#555" BorderThickness="1">
                                <Ellipse x:Name="SwitchKnob" Width="16" Height="16" Fill="#AAA" HorizontalAlignment="Left" Margin="1"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchBorder" Property="Background" Value="#007ACC"/>
                                <Setter TargetName="SwitchBorder" Property="BorderBrush" Value="#007ACC"/>
                                <Setter TargetName="SwitchKnob" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="SwitchKnob" Property="Fill" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="SwitchBorder" Property="Opacity" Value="0.5"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Privacy Settings" FontSize="28" FontWeight="Bold" Foreground="Cyan" Margin="0,0,0,25">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Cyan" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                    </TextBlock.Effect>
                </TextBlock>

                <!-- Protections Section -->
                <Border Background="#252526" CornerRadius="5" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="System Protections" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                        <TextBlock Text="Manage critical system privacy and security features." Foreground="#AAAAAA" Margin="0,0,0,20"/>
                        
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="VPN Services" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Disable background VPN services" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <CheckBox x:Name="VpnToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" Click="VpnToggle_Click"/>
                        </Grid>
                        
                        <Separator Background="#333" Margin="0,5,0,10"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="WiFi Direct" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Disable peer-to-peer wireless features" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <CheckBox x:Name="WifiDirectToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" Click="WifiDirectToggle_Click"/>
                        </Grid>
                        
                        <Separator Background="#333" Margin="0,5,0,10"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Kernel Debug Mode" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Security risk if enabled on production systems" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <CheckBox x:Name="DebugToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" Click="DebugToggle_Click"/>
                        </Grid>

                        <Separator Background="#333" Margin="0,5,0,10"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Windows Usage Data" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Telemetry and data collection (UsageDataReceiver)" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <CheckBox x:Name="UsageDataToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" Click="UsageDataToggle_Click"/>
                        </Grid>

                        <Separator Background="#333" Margin="0,5,0,10"/>
                        
                        <!-- Tamper Protection Status -->
                        <Grid Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="Windows Defender Tamper Protection" FontWeight="Bold" Foreground="White"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Status: " Foreground="Gray"/>
                                    <TextBlock x:Name="TamperProtectionText" Text="Loading..." FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                    </StackPanel>
                </Border>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,20">
                    <TextBlock x:Name="StatusDetailsText" Text="" Foreground="Gray" FontStyle="Italic"/>
                </StackPanel>



            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
