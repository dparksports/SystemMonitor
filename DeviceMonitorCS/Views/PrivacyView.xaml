<UserControl x:Class="DeviceMonitorCS.Views.PrivacyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Privacy Settings" FontSize="24" FontWeight="Bold" Foreground="Cyan" Margin="0,0,0,20">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Cyan" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>

            <GroupBox Header="System Protections" Foreground="White" BorderBrush="#3E3E42" Padding="15">
                <StackPanel>
                    <TextBlock Text="Manage critical system privacy and security features." Foreground="#CCCCCC" Margin="0,0,0,15"/>
                    
                    <CheckBox x:Name="VpnToggle" Content="VPN Services" Style="{StaticResource {x:Type CheckBox}}" Click="VpnToggle_Click" Margin="0,5"/>
                    <CheckBox x:Name="WifiDirectToggle" Content="WiFi Direct" Style="{StaticResource {x:Type CheckBox}}" Click="WifiDirectToggle_Click" Margin="0,5"/>
                    <CheckBox x:Name="DebugToggle" Content="Kernel Debug Mode" Style="{StaticResource {x:Type CheckBox}}" Click="DebugToggle_Click" Margin="0,5"/>
                    <CheckBox x:Name="UsageDataToggle" Content="Windows Usage Data Collection" Style="{StaticResource {x:Type CheckBox}}" Click="UsageDataToggle_Click" Margin="0,5"/>

                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                         <TextBlock Text="Status: " Foreground="White" VerticalAlignment="Center"/>
                         <TextBlock x:Name="StatusDetailsText" Text="Ready" Foreground="Gray" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="AI Insight (Gemini)" Foreground="White" BorderBrush="#3E3E42" Margin="0,20,0,0" Padding="15">
                <StackPanel>
                     <TextBlock Text="What does UsageDataReceiver do?" FontWeight="Bold" Foreground="Cyan" Margin="0,0,0,10"/>
                     <TextBlock x:Name="AiExplanationText" Text="Loading explanation..." TextWrapping="Wrap" Foreground="#DDDDDD" FontSize="14" Margin="0,0,0,10"/>
                     <Button x:Name="RefreshAiBtn" Content="Ask AI" HorizontalAlignment="Left" Padding="15,5" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" Click="RefreshAiBtn_Click"/>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </Grid>
</UserControl>
