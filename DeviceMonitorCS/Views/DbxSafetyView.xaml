<UserControl x:Class="DeviceMonitorCS.Views.DbxSafetyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="#0F172A">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Status / Action -->
            <RowDefinition Height="*"/>    <!-- Results Grid -->
            <RowDefinition Height="Auto"/> <!-- Remediation -->
            <RowDefinition Height="Auto"/> <!-- Legend -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="UEFI DBX SAFETY &amp; INTEGRITY" Foreground="#00F0FF" FontSize="24" FontWeight="Bold" FontFamily="Segoe UI"/>
            <TextBlock Text="Inspect EFI partition, verify bootloader signatures, and check against revocation list (DBX)." Foreground="#94A3B8" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Command Bar -->
        <Border Grid.Row="1" Background="#1E293B" CornerRadius="8" Padding="15" Margin="0,0,0,20" BorderBrush="#33FFFFFF" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="RunCheckBtn" Grid.Column="0" Content="RUN SAFETY CHECK" Click="RunCheckBtn_Click" 
                        Width="180" Height="40" Background="#00F0FF" Foreground="#0F172A" FontWeight="Bold" Cursor="Hand"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock x:Name="StatusText" Text="Ready to scan." Foreground="#E2E8F0" FontSize="14"/>
                    <TextBlock x:Name="BaselineStatusText" Text="Baseline: Not Checked" Foreground="#64748B" FontSize="12"/>
                    
                    <!-- DBX File Info -->
                    <Border Margin="0,8,0,0" Padding="10,5" Background="#11FFFFFF" CornerRadius="4" BorderBrush="#22FFFFFF" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Local DBX Verification" Foreground="#00F0FF" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                            
                            <!-- Source -->
                            <Grid Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Source:" Foreground="#94A3B8" FontSize="10"/>
                                <TextBox x:Name="DbxStatusSource" Grid.Column="1" Text="-" Foreground="#E2E8F0" FontSize="10" 
                                         Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" 
                                         ToolTip="Official Microsoft UEFI Secure Boot Objects Repository"/>
                            </Grid>
                            <TextBlock Text="(Verified Official Microsoft Repository)" Foreground="#64748B" FontSize="9" Margin="60,1,0,3" FontStyle="Italic"/>

                            <!-- Date -->
                            <Grid Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Published:" Foreground="#94A3B8" FontSize="10"/>
                                <TextBlock x:Name="DbxStatusDate" Grid.Column="1" Text="-" Foreground="#E2E8F0" FontSize="10"/>
                            </Grid>

                            <!-- Count -->
                            <Grid Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Entires:" Foreground="#94A3B8" FontSize="10"/>
                                <TextBlock x:Name="DbxStatusCount" Grid.Column="1" Text="-" Foreground="#E2E8F0" FontSize="10"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                 <!-- Badge -->
                <Border x:Name="SafetyBadge" Grid.Column="2" Background="#2294A3B8" CornerRadius="4" Padding="15,5" Visibility="Collapsed">
                    <TextBlock x:Name="SafetyBadgeText" Text="UNKNOWN" FontWeight="Bold" Foreground="#94A3B8"/>
                </Border>
            </Grid>
        </Border>

        <!-- Results Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/> <!-- Bootloader Info -->
                <ColumnDefinition Width="1*"/> <!-- EFI File List -->
            </Grid.ColumnDefinitions>

            <!-- Bootloader Details -->
            <GroupBox Grid.Column="0" Header="Bootloader Analysis (bootmgfw.efi)" Margin="0,0,10,0" Foreground="#94A3B8" BorderBrush="#33FFFFFF">
                <DataGrid x:Name="BootloaderGrid" Margin="5" Background="Transparent" BorderThickness="0" HeadersVisibility="None"  GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Key}" Width="150" Foreground="#00F0FF" FontWeight="SemiBold"/>
                        <DataGridTextColumn Binding="{Binding Value}" Width="*" Foreground="#E2E8F0"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- EFI Files Inspection -->
            <GroupBox Grid.Column="1" Header="EFI Partition Files" Margin="10,0,0,0" Foreground="#94A3B8" BorderBrush="#33FFFFFF">
                <DataGrid x:Name="EfiFilesGrid" Margin="5" Background="Transparent" BorderThickness="0"  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                         <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="60" Foreground="{Binding StatusColor}"/>
                         <DataGridTextColumn Header="File Path" Binding="{Binding Path}" Width="*" Foreground="#E2E8F0"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
        
        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#AA0F172A" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="&#xE9F3;" FontFamily="Segoe MDL2 Assets" FontSize="40" Foreground="#00F0FF" HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                         <RotateTransform x:Name="SpinnerToken" Angle="0" />
                    </TextBlock.RenderTransform>
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="SpinnerToken" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
                <TextBlock Text="Scanning System..." Foreground="#E2E8F0" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Remediation Panel (Visible on Alert) -->
        <Border x:Name="RemediationPanel" Grid.Row="3" Margin="0,15,0,0" Background="#33FF4444" CornerRadius="4" Padding="15" BorderBrush="#FF4444" BorderThickness="1" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="⚠️ SECURITY REMEDIATION OPTIONS" Foreground="#FF4444" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Option A: Direct Repair -->
                    <StackPanel Grid.Column="0">
                        <Button x:Name="RepairBootBtn" Content="REPAIR BOOTLOADER" Click="RepairBootBtn_Click" 
                                Height="35" Background="#FF4444" Foreground="White" FontWeight="Bold" Cursor="Hand"
                                BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <TextBlock Text="Recommended for Bootkits" Foreground="#E2E8F0" FontWeight="SemiBold" Margin="0,5,0,0"/>
                        <TextBlock Text="Action: Reinstalls clean Microsoft Bootloader files from your generic Windows System store." Foreground="#94A3B8" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Option B: Recovery Handoff -->
                    <StackPanel Grid.Column="2">
                        <Button x:Name="RecoveryBootBtn" Content="OPEN RECOVERY SETTINGS" Click="RecoveryBootBtn_Click" 
                                Height="35" Background="#33FFFFFF" Foreground="#E2E8F0" FontWeight="Bold" Cursor="Hand"
                                BorderBrush="#E2E8F0" BorderThickness="1">
                             <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <TextBlock Text="Manual Troubleshooting" Foreground="#E2E8F0" FontWeight="SemiBold" Margin="0,5,0,0"/>
                        <TextBlock Text="Action: Opens Windows Settings to access Advanced Startup, PC Reset, or Safe Mode." Foreground="#94A3B8" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Legend / Info Footer -->
        <Border x:Name="AlertLegend" Grid.Row="4" Margin="0,15,0,0" Background="#1E293B" CornerRadius="4" Padding="15" BorderBrush="#33FFFFFF" BorderThickness="1" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="Understanding Status Alerts" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,10" FontSize="14"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Hash Mismatch -->
                    <StackPanel x:Name="LegendHash" Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="[Hash Mismatch]" Foreground="#FF4444" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="Meaning: File content has changed, but filename is same." Foreground="#E2E8F0" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,2"/>
                        <TextBlock Text="Scenario: The 'Fingerprint' check. If bootmgfw.efi changes without an update, it is a critical Red Flag (Bootkit)." Foreground="#94A3B8" FontSize="11" TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Size Change -->
                    <StackPanel x:Name="LegendSize" Grid.Column="1" Margin="10,0,10,0">
                        <TextBlock Text="[Size Change]" Foreground="#FFAA00" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="Meaning: File grew or shrank significantly." Foreground="#E2E8F0" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,2"/>
                        <TextBlock Text="Scenario: Malware often appends code to files. A sudden size jump in a Locked System File (like BCD) is suspicious." Foreground="#94A3B8" FontSize="11" TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Timestamp -->
                    <StackPanel x:Name="LegendTime" Grid.Column="2" Margin="10,0,0,0">
                        <TextBlock Text="[Timestamp]" Foreground="#E2E8F0" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="Meaning: Content same, but 'Last Modified' changed." Foreground="#E2E8F0" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,2"/>
                        <TextBlock Text="Scenario: Benign for Log files. Suspicious for static drivers (Timestomping)." Foreground="#94A3B8" FontSize="11" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
                
                <!-- Locked Note -->
                <StackPanel x:Name="LegendLock" Orientation="Horizontal" Margin="0,15,0,0">
                    <TextBlock Text="[Locked]" Foreground="#00FF00" FontWeight="SemiBold" Margin="0,0,5,0"/>
                    <TextBlock Text=": Verified. Protected by Windows Kernel (Expected Behavior)." Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
