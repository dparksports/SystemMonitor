<UserControl x:Class="DeviceMonitorCS.Views.NetworkAdaptersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Network Adapters" FontSize="24" Foreground="White" Margin="0,0,0,20"/>

        <!-- Toolbar -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="RefreshAdaptersBtn" Content="Refresh" Background="#007ACC"/>
            <StackPanel Orientation="Horizontal" Margin="10,0">
                <TextBlock Text="Quick Actions:" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,10,0"/>
                <Button x:Name="DisableWifiDirectBtn" Content="Disable WiFi Direct"/>
                <Button x:Name="DisableWanMiniportBtn" Content="Disable WAN Miniport"/>
            </StackPanel>
        </WrapPanel>

        <!-- Grid -->
        <Border Grid.Row="2" Background="#252526" CornerRadius="5" Padding="5">
            <DataGrid x:Name="NetworkAdaptersGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      Background="Transparent" BorderThickness="0" Foreground="White" RowBackground="#252526" AlternatingRowBackground="#2D2D30">
                <DataGrid.ContextMenu>
                     <ContextMenu>
                        <MenuItem Header="Ask AI about this" Click="AskAi_Click"/>
                     </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="250"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="MAC" Binding="{Binding MacAddress}" Width="120"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding InterfaceType}" Width="100"/>
                    <DataGridTextColumn Header="DeviceID" Binding="{Binding DeviceID}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
