<UserControl x:Class="DeviceMonitorCS.Views.FirmwareSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Firmware Settings" FontSize="24" FontWeight="Bold" Foreground="Cyan" Margin="0,0,0,20">
            <TextBlock.Effect>
                <DropShadowEffect Color="#00FFFF" BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- Section 1: UEFI Variables -->
        <TextBlock Grid.Row="1" Text="UEFI Variables (MS_SystemFirmwareTable)" FontSize="16" Foreground="#CCCCCC" Margin="0,0,0,10" FontWeight="SemiBold"/>
        
        <DataGrid Grid.Row="2" x:Name="FirmwareGrid" AutoGenerateColumns="False" HeadersVisibility="Column" 
                  IsReadOnly="True" CanUserAddRows="False" Background="#252526" RowBackground="#252526" 
                  AlternatingRowBackground="#2D2D30" Foreground="White" BorderBrush="#3E3E42" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Table Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Table ID" Binding="{Binding TableID}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300"/>
                <DataGridTextColumn Header="Length (Bytes)" Binding="{Binding Length}" Width="150"/>
            </DataGrid.Columns>
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333337"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="#3E3E42"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="View Content" Click="ViewContent_Click"/>
                    <Separator/>
                    <MenuItem Header="Ask AI about this" Click="AskAi_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!-- Section 2: Boot Configuration Data -->
        <TextBlock Grid.Row="3" Text="Boot Configuration Data (BCD)" FontSize="16" Foreground="#CCCCCC" Margin="0,20,0,10" FontWeight="SemiBold"/>
        
        <DataGrid Grid.Row="4" x:Name="BcdGrid" AutoGenerateColumns="False" HeadersVisibility="Column" 
                  IsReadOnly="True" CanUserAddRows="False" Background="#252526" RowBackground="#252526" 
                  AlternatingRowBackground="#2D2D30" Foreground="White" BorderBrush="#3E3E42" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="150"/>
                <DataGridTextColumn Header="Identifier" Binding="{Binding Identifier}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Device" Binding="{Binding Device}" Width="150"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
                <DataGridTextColumn Header="Additional Settings" Binding="{Binding AdditionalSettings}" Width="300"/>
            </DataGrid.Columns>
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333337"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="#3E3E42"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Ask AI about this" Click="AskAi_Click"/>
                    <Separator/>
                    <MenuItem Header="View Details" Click="ViewDetails_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

    </Grid>
</UserControl>
