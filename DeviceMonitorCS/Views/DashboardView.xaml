<UserControl x:Class="DeviceMonitorCS.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <Style x:Key="MetricCard" TargetType="Border">
            <Setter Property="Background" Value="#CC1E293B"/>
            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style x:Key="GlowTopLine" TargetType="Border">
            <Setter Property="Height" Value="2"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="CornerRadius" Value="10,10,0,0"/>
            <Setter Property="Margin" Value="1,1,1,0"/>
        </Style>

        <Style x:Key="GlowBottomLine" TargetType="Border">
            <Setter Property="Height" Value="2"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="CornerRadius" Value="0,0,10,10"/>
            <Setter Property="Margin" Value="1,0,1,1"/>
        </Style>

        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="220"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Row: Metrics -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- CPU Usage -->
            <Border Grid.Column="0" Style="{StaticResource MetricCard}">
                <Grid>
                    <TextBlock Text="CPU Usage" Style="{StaticResource CardTitle}"/>
                    <Border Style="{StaticResource StatusBadge}" Background="#1500F0FF" BorderBrush="#3300F0FF" BorderThickness="1">
                        <TextBlock x:Name="CpuText" Text="0%" Foreground="#00F0FF" FontWeight="Bold"/>
                    </Border>
                    
                    <!-- Chart Background (Grid Lines) -->
                    <Grid Margin="0,40,0,25" ClipToBounds="False">
                        <StackPanel VerticalAlignment="Stretch">
                            <Border BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Height="33"/>
                            <Border BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Height="33"/>
                            <Border BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Height="33"/>
                        </StackPanel>
                        <!-- Y-Axis Labels (higher Z-order) -->
                        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="5,0,0,0" Panel.ZIndex="10">
                            <TextBlock Text="60" Foreground="#BBE2E8F0" FontSize="10" Height="33"/>
                            <TextBlock Text="40" Foreground="#BBE2E8F0" FontSize="10" Height="33"/>
                            <TextBlock Text="20" Foreground="#BBE2E8F0" FontSize="10" Height="33"/>
                            <TextBlock Text="0" Foreground="#BBE2E8F0" FontSize="10"/>
                        </StackPanel>

                        <!-- The Graph -->
                        <Path x:Name="CpuPath" Stroke="#00F0FF" StrokeThickness="2" Margin="30,0,0,0">
                            <Path.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="#4000F0FF" Offset="0"/>
                                    <GradientStop Color="#0000F0FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Path.Fill>
                        </Path>
                        
                        <!-- X-Axis Labels -->
                        <UniformGrid Columns="6" VerticalAlignment="Bottom" Margin="30,0,0,-25" Panel.ZIndex="10">
                            <TextBlock Text="00" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="10" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="20" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="30" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="40" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="50" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                        </UniformGrid>
                    </Grid>
                </Grid>
            </Border>

            <!-- Memory Usage -->
            <Border Grid.Column="1" Style="{StaticResource MetricCard}">
                <Grid>
                    <TextBlock Text="Memory Usage" Style="{StaticResource CardTitle}"/>
                    <Border Style="{StaticResource StatusBadge}" Background="#15FFAE00" BorderBrush="#33FFAE00" BorderThickness="1">
                        <TextBlock x:Name="MemText" Text="0 GB" Foreground="#FFAE00" FontWeight="Bold"/>
                    </Border>
                    
                    <!-- Chart Background (Grid Lines) -->
                    <Grid Margin="0,40,0,25" ClipToBounds="False">
                        <StackPanel VerticalAlignment="Stretch">
                            <Border BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Height="33"/>
                            <Border BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Height="33"/>
                            <Border BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Height="33"/>
                        </StackPanel>
                        <!-- Y-Axis Labels -->
                        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="5,0,0,0" Panel.ZIndex="10">
                            <TextBlock Text="60" Foreground="#BBE2E8F0" FontSize="10" Height="33"/>
                            <TextBlock Text="40" Foreground="#BBE2E8F0" FontSize="10" Height="33"/>
                            <TextBlock Text="20" Foreground="#BBE2E8F0" FontSize="10" Height="33"/>
                            <TextBlock Text="0" Foreground="#BBE2E8F0" FontSize="10"/>
                        </StackPanel>

                        <!-- The Graph -->
                        <Path x:Name="MemPath" Stroke="#FFAE00" StrokeThickness="2" Margin="30,0,0,0">
                             <Path.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="#40FFAE00" Offset="0"/>
                                    <GradientStop Color="#00FFAE00" Offset="1"/>
                                </LinearGradientBrush>
                            </Path.Fill>
                        </Path>

                        <!-- X-Axis Labels -->
                        <UniformGrid Columns="6" VerticalAlignment="Bottom" Margin="30,0,0,-25" Panel.ZIndex="10">
                            <TextBlock Text="00" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="10" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="20" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="30" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="40" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="50" Foreground="#BBE2E8F0" FontSize="10" HorizontalAlignment="Center"/>
                        </UniformGrid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom Row: Timeline & Status -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Security Timeline -->
            <Border Grid.Column="0" Style="{StaticResource MetricCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Security Timeline" Style="{StaticResource CardTitle}"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                        <ItemsControl x:Name="TimelineList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Dot -->
                                        <Ellipse Width="8" Height="8" Fill="{Binding ColorBrush}" VerticalAlignment="Top" Margin="5,6,0,0"/>
                                        <!-- Line -->
                                        <Rectangle Width="1" Fill="#33FFFFFF" HorizontalAlignment="Center" Margin="0,14,0,-15" Grid.Column="0"/>
                                        
                                        <!-- Content -->
                                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding Title}" Foreground="#E2E8F0" FontSize="13"/>
                                            <TextBlock Text="{Binding TimeDisplay}" Foreground="#64748B" FontSize="11"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Status Column -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Windows Defender -->
                <Border Grid.Row="0" Style="{StaticResource MetricCard}" Margin="0,0,0,5">
                    <Grid>
                        <!-- Inner Radiant Glow (Top-Center, Narrow) -->
                        <Border CornerRadius="10">
                            <Border.Background>
                                <RadialGradientBrush Center="0.5,0" GradientOrigin="0.5,0" RadiusX="0.5" RadiusY="0.7">
                                    <GradientStop Color="#504ADE80" Offset="0"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- Top Radiant Line -->
                        <Border Style="{StaticResource GlowTopLine}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                    <GradientStop Color="Transparent" Offset="0"/>
                                    <GradientStop Color="#4ADE80" Offset="0.5"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <!-- Bottom Radiant Line -->
                        <Border Style="{StaticResource GlowBottomLine}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                    <GradientStop Color="Transparent" Offset="0"/>
                                    <GradientStop Color="#4ADE80" Offset="0.5"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <Grid VerticalAlignment="Center" Margin="10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" Width="48" Height="48" Margin="0,0,15,0">
                                <Ellipse Fill="#4ADE80"/>
                                <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#0F172A" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                            </Grid>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                 <TextBlock Text="Windows Defender:" Foreground="#E2E8F0" FontSize="14"/>
                                 <TextBlock Text="Active" Foreground="#4ADE80" FontSize="22" FontWeight="Bold" Margin="0,-2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Firewall -->
                <Border Grid.Row="1" Style="{StaticResource MetricCard}" Margin="0,5,0,0">
                    <Grid>
                        <!-- Inner Radiant Glow (Top-Center, Narrow) -->
                        <Border CornerRadius="10">
                            <Border.Background>
                                <RadialGradientBrush Center="0.5,0" GradientOrigin="0.5,0" RadiusX="0.5" RadiusY="0.7">
                                    <GradientStop Color="#50F59E0B" Offset="0"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>

                        <!-- Top Radiant Line -->
                        <Border Style="{StaticResource GlowTopLine}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                    <GradientStop Color="Transparent" Offset="0"/>
                                    <GradientStop Color="#F59E0B" Offset="0.5"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <!-- Bottom Radiant Line -->
                        <Border Style="{StaticResource GlowBottomLine}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                    <GradientStop Color="Transparent" Offset="0"/>
                                    <GradientStop Color="#F59E0B" Offset="0.5"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <Grid VerticalAlignment="Center" Margin="10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" Width="48" Height="48" Margin="0,0,15,0">
                                <Ellipse Fill="#F59E0B"/>
                                <TextBlock Text="&#xE727;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#0F172A" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                            </Grid>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                 <TextBlock Text="Firewall Status:" Foreground="#E2E8F0" FontSize="14"/>
                                 <TextBlock Text="Strict" Foreground="#F59E0B" FontSize="22" FontWeight="Bold" Margin="0,-2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
