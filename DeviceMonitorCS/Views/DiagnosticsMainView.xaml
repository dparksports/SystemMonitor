<UserControl x:Class="DeviceMonitorCS.Views.DiagnosticsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sub-Navigation -->
        <Border Grid.Column="0" Background="#11FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="0,0,1,0" Margin="0,0,0,0">
            <StackPanel Margin="10,20,10,10">
                <TextBlock Text="DIAGNOSTIC TOOLS" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="10,0,0,15"/>
                
                <Button x:Name="PerfBtn" Content="System Performance" Click="PerfBtn_Click" Style="{StaticResource NavButton}"/>
                <Button x:Name="TimelineBtn" Content="System Timeline" Click="TimelineBtn_Click" Style="{StaticResource NavButton}"/>
                <Button x:Name="FirmwareBtn" Content="Firmware Settings" Click="FirmwareBtn_Click" Style="{StaticResource NavButton}"/>
                <Button x:Name="DeviceMgmtBtn" Content="Device Management" Click="DeviceMgmtBtn_Click" Style="{StaticResource NavButton}"/>
                <Button x:Name="ShutdownBtn" Content="True Shutdown" Click="ShutdownBtn_Click" Style="{StaticResource NavButton}"/>
            </StackPanel>
        </Border>

        <!-- Sub-Content -->
        <Border Grid.Column="1" Margin="20">
            <ContentControl x:Name="SubContentArea"/>
        </Border>
    </Grid>
</UserControl>
