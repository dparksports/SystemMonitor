<Window x:Class="DeviceMonitorCS.TasksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Scheduled Tasks Manager" Height="500" Width="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <!-- Toolbar -->
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button x:Name="DisableBtn" Content="Disable" Margin="5" Width="70"/>
                <Button x:Name="StopBtn" Content="Stop" Margin="5" Width="70"/>
                <Button x:Name="StartBtn" Content="Start" Margin="5" Width="70"/>
                <Button x:Name="DeleteBtn" Content="Delete" Margin="5" Width="70"/>
                <Separator/>
                <Button x:Name="RefreshBtn" Content="Refresh" Margin="5" Width="70"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Tasks Grid -->
        <GroupBox DockPanel.Dock="Top" Header="Scheduled Tasks Running as Admin" Padding="5" Margin="5">
            <DataGrid x:Name="TasksGrid" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Task Name" Binding="{Binding TaskName}" Width="250"/>
                    <DataGridTextColumn Header="State" Binding="{Binding State}" Width="80"/>
                    <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="*"/>
                    <DataGridTextColumn Header="User" Binding="{Binding User}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </DockPanel>
</Window>
