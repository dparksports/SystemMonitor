<Window x:Class="DeviceMonitorCS.WanMiniportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WAN Miniport Manager" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button x:Name="DisableSstpBtn" Content="Disable SstpSvc" Margin="5"/>
                <Button x:Name="StopSstpBtn" Content="Stop SstpSvc" Margin="5"/>
                <Button x:Name="StartSstpBtn" Content="Start SstpSvc" Margin="5"/>
                <Separator/>
                <Button x:Name="DisableAllWanBtn" Content="Disable All WAN Miniports" Margin="5" Background="#FFFFE0E0"/>
                <Button x:Name="UninstallAllWanBtn" Content="Uninstall All WAN Miniports" Margin="5" Background="#FFFFC0C0"/>
                <Separator/>
                <Button x:Name="RefreshBtn" Content="Refresh" Margin="5"/>
            </ToolBar>
        </ToolBarTray>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>

        <GroupBox Header="WAN Miniport Adapters" Margin="5">
             <DataGrid x:Name="WanGrid" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="250"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Interface Type" Binding="{Binding InterfaceType}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </DockPanel>
</Window>
