<Window x:Class="DeviceMonitorCS.AskAiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ask AI Assistant" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Context:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="ContextBox" Grid.Row="1" Height="80" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="#F0F0F0" Padding="5"/>

        <TextBlock Text="AI Response:" Grid.Row="2" FontWeight="Bold" Margin="0,10,0,5" VerticalAlignment="Top"/>
        <Grid Grid.Row="2" Margin="0,25,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="ResponseBox" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="5"/>
            <StackPanel x:Name="ActionsPanel" Grid.Row="1" Orientation="Vertical" Margin="0,5,0,0"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QuestionBox" Height="30" VerticalContentAlignment="Center" Padding="5" Text="What is this?"/>
            <Button x:Name="AskBtn" Grid.Column="1" Content="Ask Gemini" Width="80" Height="30" Margin="5,0,0,0" IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
